import 'package:flutter/material.dart';

/// Tableau de bord Conducteur (statut en ligne/hors ligne, courses reçues).
/// Contenu à implémenter dans une itération ultérieure.
class ConducteurHomePage extends StatelessWidget {
  const ConducteurHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Espace Conducteur')),
      body: const Center(child: Text('Interface Conducteur - à venir')),
    );
  }
}
