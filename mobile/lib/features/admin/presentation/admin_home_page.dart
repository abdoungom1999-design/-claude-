import 'package:flutter/material.dart';

/// Tableau de bord Admin (supervision clients, conducteurs, courses).
/// Contenu à implémenter dans une itération ultérieure.
class AdminHomePage extends StatelessWidget {
  const AdminHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Espace Admin')),
      body: const Center(child: Text('Interface Admin - à venir')),
    );
  }
}
